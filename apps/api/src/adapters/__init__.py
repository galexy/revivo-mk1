"""Adapters layer - infrastructure implementations.

Contains implementations of domain ports (interfaces) using specific
infrastructure technologies:
- persistence: Database adapters using SQLAlchemy
- api: HTTP/REST adapters using FastAPI (future)
- security: Encryption and authentication (future)
"""
