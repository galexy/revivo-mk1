{
  "name": "api",
  "projectType": "application",
  "sourceRoot": "apps/api/src",
  "implicitDependencies": [
    "domain"
  ],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run --package personal-finance-api fastapi dev src/adapters/api/app.py --host 0.0.0.0",
        "cwd": "{projectRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run --package personal-finance-api pytest tests/ -v --tb=short",
        "cwd": "{projectRoot}"
      },
      "inputs": [
        "{projectRoot}/**/*.py",
        "{workspaceRoot}/libs/domain/**/*.py"
      ],
      "outputs": []
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run --package personal-finance-api ruff check src/ tests/",
        "cwd": "{projectRoot}"
      },
      "inputs": [
        "{projectRoot}/**/*.py"
      ],
      "outputs": []
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run --package personal-finance-api pyright",
        "cwd": "{projectRoot}"
      },
      "inputs": [
        "{projectRoot}/**/*.py",
        "{workspaceRoot}/libs/domain/**/*.py"
      ],
      "outputs": []
    },
    "coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run --package personal-finance-api pytest tests/ -v --tb=short --cov=src --cov-config=../../pyproject.toml --cov-report=term-missing --cov-branch",
        "cwd": "{projectRoot}"
      },
      "inputs": [
        "{projectRoot}/**/*.py",
        "{workspaceRoot}/libs/domain/**/*.py"
      ],
      "outputs": []
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run --package personal-finance-api ruff format --check src/ tests/",
        "cwd": "{projectRoot}"
      },
      "inputs": ["{projectRoot}/**/*.py"],
      "outputs": []
    }
  }
}
